package DecisionTree;

import Util.Pair;

public class DTPrunable extends DTree implements Cloneable {
	@Override
	protected Object clone() throws CloneNotSupportedException {
		return super.clone();
	}
	
}
